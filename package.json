{
  "name": "jeong.site",
  "private": true,
  "version": "0.0.1",
  "packageManager": "pnpm@9.12.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "echo \"(placeholder) apps/web dev will run here later\"",
    "build": "echo \"(placeholder) monorepo build\"",
    "lint": "echo \"(placeholder) monorepo lint\"",
    "typecheck": "echo \"(placeholder) monorepo typecheck\""
  }
}
